@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";    
}


@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    <script>
        $(document).ready(function () {
            Download_BC_DA(4217);
        });
        function Download_BC_DA(id) {
            var pJson = { 'id': id };
            jQuery.ajax({
                url: "https://thamvan.monre.gov.vn/Home/Download_BC_DA",
                contentType: 'application/json',
                dataType: 'json',
                data: JSON.stringify(pJson),
                type: "POST",
                success: function (r) {
                    for (var i = 0; i < r.file.length; i++) {
                        window.location.href = SiteUrl + r.file[i].Path
                    }

                },
                error: function (ex) {
                    w.close();

                }
            });
        }
    </script>
}
